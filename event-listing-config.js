// The public Shows page and the admin placement editor share this value.
// Pagination is always derived from the canonical event display order.
(() => {
  window.iVenueEventListing = Object.freeze({
    pageSize: 9,
  });
})();
